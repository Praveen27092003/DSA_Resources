var climbStairs = function(n) {
    
    if(n<=2){
        return n
    }
    
    // let arr = new Array(n+1)
    // arr[0]=1
    // arr[1]=1

//     for(i=2;i<=n;i++){
//         arr[i]=arr[i-1]+arr[i-2];

//     }
//             return arr[n]
    
    let a =2    
    let b = 1
    
    for(let i=0; i<n-2;i++){
        a =a+b
        b = a-b
    }
    return a
};

console.log(climbStairs(5))